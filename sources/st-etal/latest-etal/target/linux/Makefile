ODIR   = ${realpath .}/o
CURDIR = ${realpath .}
ROOTDIR= ${realpath ../../}

# variable for etalmdr makefile
ifndef WSPATH
WSPATH=${realpath ../../etalmdr}
endif
ifndef TARGETPATH
TARGETPATH=${realpath ../}
endif

include ../../Makefile
